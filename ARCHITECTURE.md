# StudentMezo DApp - Architecture Overview

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND LAYER                          │
│                     (React + Vite + Wagmi)                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │  Home Page   │  │   Student    │  │   Lender     │        │
│  │              │  │  Dashboard   │  │  Dashboard   │        │
│  └──────────────┘  └──────────────┘  └──────────────┘        │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │              RainbowKit Wallet Connection                 │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │         Wagmi Hooks (useReadContract, useWrite)          │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      BLOCKCHAIN LAYER                           │
│                   (Ethereum / Testnet)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────┐  ┌──────────────────┐  ┌──────────────┐ │
│  │   MUSD Token    │  │  StudentLoanNFT  │  │   Platform   │ │
│  │   (ERC20)       │  │    (ERC721)      │  │   Contract   │ │
│  │                 │  │                  │  │              │ │
│  │ • Mint          │  │ • Achievements   │  │ • Loans      │ │
│  │ • Burn          │  │ • Metadata       │  │ • Pool       │ │
│  │ • Transfer      │  │ • User NFTs      │  │ • Verify     │ │
│  └─────────────────┘  └──────────────────┘  └──────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      EXTERNAL LAYER                             │
│                  (Future Integrations)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐   │
│  │  Mezo SDK   │  │  Chainlink  │  │  Identity Providers │   │
│  │  (Bitcoin)  │  │   Oracles   │  │  (Verification)     │   │
│  └─────────────┘  └─────────────┘  └─────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## Component Interaction Flow

### Student Loan Request Flow

```
┌─────────┐
│ Student │
└────┬────┘
     │
     │ 1. Connect Wallet
     ▼
┌─────────────────┐
│  RainbowKit UI  │
└────┬────────────┘
     │
     │ 2. Get Verified
     ▼
┌──────────────────────┐      ┌────────────────┐
│ Platform Contract    │◄─────│   Verifier     │
│ verifyStudent()      │      │   (Admin)      │
└──────┬───────────────┘      └────────────────┘
       │
       │ 3. Deposit Collateral & Mint MUSD
       ▼
┌──────────────────────────────────────┐
│ depositCollateralAndMintMUSD()       │
│   • Receive ETH                      │
│   • Call MUSD.mint()                 │
│   • Emit Events                      │
└──────┬───────────────────────────────┘
       │
       │ 4. Request Loan
       ▼
┌──────────────────────────────────────┐
│ requestLoan(amount, duration, purpose)│
│   • Create Loan struct               │
│   • Calculate interest rate          │
│   • Mint First Loan NFT              │
│   • Emit LoanRequested event         │
└──────┬───────────────────────────────┘
       │
       │ 5. Fund Loan (Admin)
       ▼
┌──────────────────────────────────────┐
│ fundLoan(loanId)                     │
│   • Transfer MUSD from pool          │
│   • Update loan status to ACTIVE     │
│   • Emit LoanFunded event            │
└──────┬───────────────────────────────┘
       │
       │ 6. Repay Loan
       ▼
┌──────────────────────────────────────┐
│ repayLoan(loanId, amount)            │
│   • Transfer MUSD from student       │
│   • Update repayment amount          │
│   • Increase reputation              │
│   • Mint achievement NFTs            │
│   • Distribute yield to lenders      │
└──────────────────────────────────────┘
```

### Lender Pool Flow

```
┌─────────┐
│ Lender  │
└────┬────┘
     │
     │ 1. Get MUSD (Mint or Deposit Collateral)
     ▼
┌──────────────┐
│  MUSD Token  │
└────┬─────────┘
     │
     │ 2. Approve Platform
     ▼
┌──────────────────────┐
│ MUSD.approve()       │
└────┬─────────────────┘
     │
     │ 3. Contribute to Pool
     ▼
┌──────────────────────────────────────┐
│ contributeToPool(amount)             │
│   • Transfer MUSD to platform        │
│   • Update lender contribution       │
│   • Emit LenderContribution event    │
└──────┬───────────────────────────────┘
       │
       │ 4. Automatic Loan Funding
       ▼
┌──────────────────────────────────────┐
│ Platform funds loans from pool       │
└──────┬───────────────────────────────┘
       │
       │ 5. Earn Yield from Repayments
       ▼
┌──────────────────────────────────────┐
│ Yield Distribution                   │
│   • Proportional to contribution     │
│   • Updated on each repayment        │
└──────────────────────────────────────┘
```

## Smart Contract Architecture

### Contract Relationships

```
┌────────────────────────────────────────────────────────┐
│              StudentLoanPlatform (Main)                │
│                                                        │
│  • Ownable (Admin functions)                          │
│  • ReentrancyGuard (Security)                         │
│  • Manages all platform logic                         │
└───────────┬────────────────────────┬───────────────────┘
            │                        │
            │                        │
            ▼                        ▼
┌───────────────────┐    ┌──────────────────────┐
│    MUSD Token     │    │   StudentLoanNFT     │
│                   │    │                      │
│  • ERC20          │    │  • ERC721            │
│  • Ownable        │    │  • Ownable           │
│  • Mintable       │    │  • Achievement NFTs  │
└───────────────────┘    └──────────────────────┘
```

### Platform Contract State

```
┌─────────────────────────────────────────────┐
│       StudentLoanPlatform State             │
├─────────────────────────────────────────────┤
│                                             │
│  Students:                                  │
│  ├── mapping(address => Student)            │
│  │   ├── isVerified: bool                   │
│  │   ├── totalBorrowed: uint256             │
│  │   ├── totalRepaid: uint256               │
│  │   ├── reputationScore: uint256           │
│  │   └── loanIds: uint256[]                 │
│                                             │
│  Loans:                                     │
│  ├── mapping(uint256 => Loan)               │
│  │   ├── id: uint256                        │
│  │   ├── student: address                   │
│  │   ├── amount: uint256                    │
│  │   ├── collateralAmount: uint256          │
│  │   ├── interestRate: uint256              │
│  │   ├── duration: uint256                  │
│  │   ├── startTime: uint256                 │
│  │   ├── amountRepaid: uint256              │
│  │   ├── status: LoanStatus                 │
│  │   └── purpose: string                    │
│                                             │
│  Lender Pool:                               │
│  ├── totalPooled: uint256                   │
│  ├── totalLent: uint256                     │
│  ├── totalReturned: uint256                 │
│  ├── contributions: mapping(address=>uint)  │
│  └── earned: mapping(address=>uint)         │
│                                             │
└─────────────────────────────────────────────┘
```

## Frontend Architecture

### Component Hierarchy

```
App
├── Navbar
│   ├── Logo
│   ├── Navigation Links
│   └── ConnectButton (RainbowKit)
│
├── HomePage
│   ├── Hero Section
│   ├── Features Grid (6 features)
│   ├── CTA Section
│   └── Stats Section
│
├── StudentDashboard
│   ├── Status Cards (4 metrics)
│   ├── Collateral Deposit Form
│   ├── Loan Request Form
│   ├── Loans List Table
│   └── Achievements Grid
│
└── LenderDashboard
    ├── Portfolio Stats (4 metrics)
    ├── Contribution Form
    ├── Pool Overview Stats
    ├── How It Works Info
    └── Active Loans List
```

### Data Flow (Wagmi)

```
┌──────────────────────────────────────────────────┐
│              React Components                    │
└────────────────┬─────────────────────────────────┘
                 │
                 │ useReadContract / useWriteContract
                 ▼
┌──────────────────────────────────────────────────┐
│              Wagmi Hooks Layer                   │
│  • Contract reads (automatic polling)            │
│  • Contract writes (transaction handling)        │
│  • Account management                            │
│  • Network detection                             │
└────────────────┬─────────────────────────────────┘
                 │
                 │ JSON-RPC calls
                 ▼
┌──────────────────────────────────────────────────┐
│              Viem Layer                          │
│  • Ethereum client                               │
│  • Transaction encoding                          │
│  • Type safety                                   │
└────────────────┬─────────────────────────────────┘
                 │
                 │ HTTP/WebSocket
                 ▼
┌──────────────────────────────────────────────────┐
│            Ethereum Network                      │
│  • Smart contracts                               │
│  • Blockchain state                              │
└──────────────────────────────────────────────────┘
```

## Security Architecture

### Access Control

```
┌─────────────────────────────────────────────────┐
│              Access Control Layers              │
├─────────────────────────────────────────────────┤
│                                                 │
│  Owner (Platform Admin)                         │
│  ├── Add/Remove verifiers                       │
│  ├── Fund loans                                 │
│  ├── Manage MUSD minters                        │
│  └── Transfer NFT ownership                     │
│                                                 │
│  Verifiers                                      │
│  ├── Verify students                            │
│  └── (Designated by owner)                      │
│                                                 │
│  Verified Students                              │
│  ├── Deposit collateral                         │
│  ├── Request loans                              │
│  └── Repay loans                                │
│                                                 │
│  Lenders (Any address)                          │
│  ├── Contribute to pool                         │
│  └── Earn yield                                 │
│                                                 │
└─────────────────────────────────────────────────┘
```

### Security Measures

```
┌─────────────────────────────────────────────────┐
│            Security Implementation              │
├─────────────────────────────────────────────────┤
│                                                 │
│  Smart Contract Level:                          │
│  ├── ReentrancyGuard on all transfers           │
│  ├── Ownable access control                     │
│  ├── OpenZeppelin audited libraries             │
│  ├── Input validation (require statements)      │
│  └── Over-collateralization (150%)              │
│                                                 │
│  Application Level:                             │
│  ├── Form validation                            │
│  ├── Transaction confirmation                   │
│  ├── Error handling                             │
│  └── User feedback                              │
│                                                 │
│  Network Level:                                 │
│  ├── HTTPS connections                          │
│  ├── Wallet encryption                          │
│  └── RPC endpoint security                      │
│                                                 │
└─────────────────────────────────────────────────┘
```

## Deployment Architecture

### Development Environment

```
┌─────────────────────────────────────────────────┐
│           Local Development Setup               │
├─────────────────────────────────────────────────┤
│                                                 │
│  Terminal 1: Hardhat Node                      │
│  ├── Local blockchain                           │
│  ├── Port 8545                                  │
│  └── 20 test accounts                           │
│                                                 │
│  Terminal 2: Deploy Contracts                   │
│  ├── Deploy MUSD                                │
│  ├── Deploy NFT                                 │
│  ├── Deploy Platform                            │
│  └── Configure permissions                      │
│                                                 │
│  Terminal 3: Frontend Dev Server                │
│  ├── Vite dev server                            │
│  ├── Port 5173                                  │
│  ├── Hot module reload                          │
│  └── Connected to local chain                   │
│                                                 │
└─────────────────────────────────────────────────┘
```

### Production Architecture

```
┌─────────────────────────────────────────────────┐
│          Production Deployment                  │
├─────────────────────────────────────────────────┤
│                                                 │
│  Smart Contracts                                │
│  ├── Deployed to Ethereum Mainnet              │
│  ├── Verified on Etherscan                     │
│  ├── Multi-sig owner address                   │
│  └── Immutable (or upgradeable proxy)          │
│                                                 │
│  Frontend                                       │
│  ├── Built with Vite                            │
│  ├── Deployed to Vercel/Netlify/IPFS           │
│  ├── CDN distribution                           │
│  └── Environment variables configured           │
│                                                 │
│  Infrastructure                                 │
│  ├── RPC nodes (Infura/Alchemy)                │
│  ├── IPFS pinning service                      │
│  ├── Monitoring (Tenderly/Defender)            │
│  └── Analytics                                  │
│                                                 │
└─────────────────────────────────────────────────┘
```

## Event-Driven Architecture

```
┌─────────────────────────────────────────────────┐
│              Smart Contract Events              │
├─────────────────────────────────────────────────┤
│                                                 │
│  StudentVerified(address student)               │
│  CollateralDeposited(address, uint256)          │
│  MUSDMinted(address, uint256)                   │
│  LoanRequested(uint256 id, address, uint256)    │
│  LoanFunded(uint256 id, uint256)                │
│  LoanRepaid(uint256 id, uint256)                │
│  LenderContribution(address, uint256)           │
│  YieldDistributed(address, uint256)             │
│  AchievementMinted(address, uint256, type)      │
│                                                 │
└────────────────┬────────────────────────────────┘
                 │
                 │ Event Emission
                 ▼
┌─────────────────────────────────────────────────┐
│           Blockchain Event Log                  │
└────────────────┬────────────────────────────────┘
                 │
                 │ Event Listening
                 ▼
┌─────────────────────────────────────────────────┐
│           Frontend (Wagmi Watch)                │
│  • Real-time updates                            │
│  • Transaction confirmations                    │
│  • State synchronization                        │
└─────────────────────────────────────────────────┘
```

## Data Models

### Student Model
```typescript
Student {
  isVerified: boolean
  totalBorrowed: uint256
  totalRepaid: uint256
  reputationScore: uint256
  loanIds: uint256[]
}
```

### Loan Model
```typescript
Loan {
  id: uint256
  student: address
  amount: uint256
  collateralAmount: uint256
  interestRate: uint256  // basis points
  duration: uint256      // seconds
  startTime: uint256
  amountRepaid: uint256
  status: LoanStatus    // PENDING, ACTIVE, REPAID, DEFAULTED
  purpose: string
}
```

### Achievement Model
```typescript
Achievement {
  achievementType: AchievementType
  timestamp: uint256
  metadata: string
}
```

## Technology Integration Points

```
┌──────────────────────────────────────────────────────────┐
│              Current Implementation                      │
├──────────────────────────────────────────────────────────┤
│  ✅ Ethereum Smart Contracts                            │
│  ✅ React Frontend                                       │
│  ✅ RainbowKit Wallet Connection                         │
│  ✅ Wagmi Blockchain Hooks                               │
│  ✅ Hardhat Development                                  │
│  ✅ OpenZeppelin Security                                │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│              Future Integration Points                   │
├──────────────────────────────────────────────────────────┤
│  ⏳ Mezo SDK (Real Bitcoin deposits)                    │
│  ⏳ Chainlink Oracles (BTC/USD price feeds)             │
│  ⏳ The Graph (Event indexing)                           │
│  ⏳ IPFS (Decentralized storage)                         │
│  ⏳ ENS (Username resolution)                            │
└──────────────────────────────────────────────────────────┘
```

---

This architecture provides a solid foundation for a scalable, secure, and user-friendly DeFi lending platform! 🏗️
